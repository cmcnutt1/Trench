<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shovel.trench">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ArtistSongSearch" />
        <activity android:name=".PlayerScreen">
            theme="@android:style/Theme.NoTitleBar"
        </activity>
        <activity android:name=".LandingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".SelectionMenu" />
        <activity android:name=".Settings" />
        <activity android:name=".PlaylistSearch" />
        <activity android:name=".LengthSelectionScreen"></activity>
    </application>

</manifest>